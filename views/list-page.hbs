{{> navbar}}

<main class="bg-gray-50 p-6 md:p-10">
    <div class="max-w-7xl mx-auto">
        <h1 class="text-3xl font-bold text-gray-800 mb-8 text-center">Последние новости</h1>
        
        <!-- Grid Container -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
            {{#each articles}}
            <div class="group relative overflow-hidden rounded-lg border border-gray-200 bg-white shadow-md hover:shadow-lg transition-all duration-300">
                <div class="aspect-w-4 aspect-h-3 overflow-hidden">
                    <img src="{{ this.photoUrl }}" 
                         alt="Nature image"
                         class="w-full h-64 object-cover transition-transform duration-500 group-hover:scale-105">
                </div>
                <div class="p-4 border-t border-gray-100">
                    <a href="/articles/{{ this.id }}" class="text-lg font-semibold text-gray-800 hover:text-blue-600 transition-colors">
                        {{ this.title }}
                        <span class="absolute inset-0" aria-hidden="true"></span>
                    </a>
                    <p class="mt-1 text-sm text-gray-500">{{ this.title }}</p>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</main>
